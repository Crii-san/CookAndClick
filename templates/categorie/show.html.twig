{% extends 'base.html.twig' %}

{% block title %}Tous les {{ categorie.nom }}s{% endblock %}

{% block body %}

    <h1>Tous les {{ categorie.nom }}s</h1>

    <ul>
        {% for recette in recettes %}
            <li><span class="recette">{{ recette.nom }}</span></li>
        {% endfor %}
    </ul>

{% endblock %}
