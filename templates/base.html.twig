<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">

        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="{{ asset('/img/favicon.ico') }}" type="image/x-icon">
        <link href="{{ asset('/css/style.css') }}" rel="stylesheet"/>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
        <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined" rel="stylesheet" />
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
        {% endblock %}
    </head>
    <body>

    <div class="page-wrap">
    <nav class="navbar navbar-expand-lg bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('app_recette')}}">Recettes</a>
            <a class="navbar-brand" href="{{ path('app_allergene')}}">Allergènes</a>
            {% if app.user %}
                <a class="navbar-brand" href="{{ path('app_categorie')}}">Catégories</a>
                <a class="navbar-brand" href="{{ path('app_recette_create')}}">Ajouter une recette</a>
            {% endif %}
            <form class="d-flex align-items-center" role="search" lang="fr" action="{{ path('app_recette')}}">
                <input class="form-control me-2" type="search" placeholder="{{ search|default('') }}" aria-label="Search" name="search">
                <button class="btn btn-dark" type="submit"><span class="material-symbols-outlined">Search</span></button>
            </form>

            {% if app.user %}
                <div class="d-flex align-items-center">
                    <a class="btn btn-dark me-2" href="{{ path('app_user_show', {'id': app.user.idUser}) }}">Hi, {{ app.user.prenom | capitalize }} !</a>
                    <a href="{{ path('app_logout') }}">
                        <span class="material-symbols-outlined" style="color: black; font-size: 2em;">Logout</span>
                    </a>
                </div>
            {% else %}
                <a href="{{ path('app_login') }}">
                    <span class="material-symbols-outlined" style="color: black; font-size: 2em;">Login</span>
                </a>
            {% endif %}
        </div>
    </nav>

    <div  class="en-tete">
        <h1 class="logo"><a href="{{ path('app_accueil')}}">Cook&Click</a></h1>
        <h2 class="logo2"><a href="{{ path('app_accueil')}}">Cook&Click</a></h2>
    </div >

    <div class="menu">
        <div class="slogan">
            <p class="slogan1">Cliquez.Preparez.Mangez!</p>
        </div>
    </div>
    </div>
    {% block body %}{% endblock %}

    <footer id="footer" class="bg-dark text-white text-center py-3 text-white mx-2">
        <a href="#" class="text-white mx-2">À PROPOS</a>
        <a href="#" class="text-white mx-2">CONTACT</a>
        <a href="#" class="text-white mx-2">NEWSLETTER</a>
        <a href="#" class="text-white mx-2">MENTIONS LÉGALES</a>
        <a href="#" class="text-white mx-2">RECRUTEMENT</a>
        <a href="#" class="text-white mx-2">CGU</a>
        <a href="#" class="text-white mx-2">PROTECTION DES DONNÉES PERSONNELLES</a>
        <a href="#" class="text-white mx-2">COOKIES</a>
        <p class="mt-3">Tous droits réservés cookandclick.fr - 2023-2024</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-kenU1KFdBIe4zVF0s0G1M5b4hcpxyD9F7jL+jjXkk+Q2h455rYXK/7HAuoJl+0I4" crossorigin="anonymous"></script>
    </body>
</html>
