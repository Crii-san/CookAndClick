{% extends 'base.html.twig' %}

{% block title %}Toutes les desserts{% endblock %}

{% block body %}

    <h1>Toutes les desserts</h1>

    <ul>
        {% for recette in recettes %}
            <li><span class="recette">{{ recette.nom }}</span></li>
        {% endfor %}
    </ul>

{% endblock %}
